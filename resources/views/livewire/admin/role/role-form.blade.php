<div>
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0">{{ $isEdit ? 'Edit User' : 'Create New Role' }}</h1>
        <a href="{{ route('admin.role.simple') }}" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Back to Roles
        </a>
    </div>
</div>
